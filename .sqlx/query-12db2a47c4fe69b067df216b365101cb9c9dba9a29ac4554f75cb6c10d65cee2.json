{
  "db_name": "PostgreSQL",
  "query": "\n    WITH updated AS (\n        UPDATE tabular\n        -- Append a suffix to the name to avoid conflicts with new tables\n        SET deleted_at = now(), name = name || $3\n        WHERE tabular_id = $1\n        AND typ = $2\n        AND metadata_location IS NOT NULL\n        AND tabular_id IN (SELECT tabular_id FROM active_tabulars)\n        RETURNING tabular_id, metadata_location\n    ),\n    deleted AS (\n        DELETE FROM tabular\n        WHERE tabular_id = $1\n        AND typ = $2\n        AND metadata_location IS NULL\n        AND tabular_id IN (SELECT tabular_id FROM active_tabulars)\n        RETURNING tabular_id, metadata_location\n    )\n    SELECT tabular_id, metadata_location FROM updated\n    UNION ALL\n    SELECT tabular_id, metadata_location FROM deleted\n    ",
  "describe": {
    "columns": [
      {
        "ordinal": 0,
        "name": "tabular_id",
        "type_info": "Uuid"
      },
      {
        "ordinal": 1,
        "name": "metadata_location",
        "type_info": "Text"
      }
    ],
    "parameters": {
      "Left": [
        "Uuid",
        {
          "Custom": {
            "name": "tabular_type",
            "kind": {
              "Enum": [
                "table",
                "view"
              ]
            }
          }
        },
        "Text"
      ]
    },
    "nullable": [
      null,
      null
    ]
  },
  "hash": "12db2a47c4fe69b067df216b365101cb9c9dba9a29ac4554f75cb6c10d65cee2"
}
